index_of(X,[X|_],0).
index_of(X,[_|T],Index) :-
    index_of(X,T,TailIndex),
    Index is TailIndex + 1.

?-index_of(4,[1,2,4,5,6],Index).

?-(5>3 ->  write('Yes');write('No')).

max_in_list([X],X).
max_in_list([H|T],Max):-
    max_in_list(T,TailMax),
	(  H > TailMax ->  Max = H;
    Max = TailMax
    ).
?-max_in_list([1,2,3,6,7],Max).
